import styled from 'styled-components'

export const StyledFooter = styled.footer`
  font-size: 21px;
  margin: 50px 0;

  div {
    font-size: 16px;
    margin-top: 20px;
  }

  a {
    color: var(--color-ch-green);
  }
`
